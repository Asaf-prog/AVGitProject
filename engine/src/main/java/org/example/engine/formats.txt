
git objects will be saved to file in the following formats:

gitCommit:

    {parentCommitHash},{secondParentCommitHash},{treeRootHash},{comment},{this.creationTime},{author}

gitBlob:

gitTree:


datetime will be represented in the following format:

"dd.MM.yyyy-HH:mm:ss:SSS"


////////////////////////////-Create a new commit-///////////////////////

we have the ability to create a new commit because the file changes => we there are some sh1 that we didn't find
in addition we have the last Header file, so we have the commit that we create from,
and then we create a head file that point on the root directory
we will get the exact file how changes and start is recursion create their blob and tree
